# Lite commands

## `php artisan app:create-admin-user`

Creates the initial administrator when none exists.

## `php artisan backup:database --verify`

Creates and verifies a database backup. Run this before any manual production data operation.

Historical import, media-library, and AI maintenance commands are outside the supported Lite
service surface.
